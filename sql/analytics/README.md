# sql/analytics/

DDL for the dimensional model (`dim_*`, `fact_*` tables) lands here once
docs/analytics-engineering-guide.md's "Analytical Data Model" section is
implemented. Empty for now — see `sql/source/` for what exists today
(the source-mirror and Bronze-ingestion-support schema).
